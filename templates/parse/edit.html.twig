{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Video Data</h1>

    {{ form_start(videoDataForm) }}
        {{ form_widget(videoDataForm) }}

        <button type="submit" class="btn btn-primary" formnovalidate>Update</button>
    {{ form_end(videoDataForm) }}

{% endblock %}